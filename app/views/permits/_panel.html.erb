<ul class="nav nav-pills permnavi">
  
  <% unless action?('index') %>
  <li class="btn">
		<%= link_to " назад", :back, :class => "" %>
	</li>
	<% end %>
  
  <% if current_user.has_permission?(6) %>
  <li class="btn">
  	<%= link_to "Новая заявка", new_permit_path, :class=> "" %>
  </li>
  <% end %>
  
  <li class=<%= (params[:scope] ? 'btn' : 'btn btn-primary')  %>>
  	<%= link_to "Все пропуска", permits_path, :class=> "dropdown-toggle" %>
  </li>

  <li class=<%= (params[:scope] == 'application' ? 'btn btn-primary' : 'btn') %>>
  	<%= link_to "Текущие заявки", permits_path(:scope => :application)%>
  </li>

  <li class=<%= (params[:scope] == 'expired' ? 'btn btn-primary' : 'btn')%>>
  	<%= link_to "Просроченные", permits_path(:scope => :expired) %>
  </li>
  
  <li class= <%= (params[:scope] == 'for_print' ? 'btn btn-primary' : 'btn') %> >
  	<%= link_to "Групповая печать пеш пропусков", permits_path(:scope => :for_print) %>
  </li>

</ul>