doctype
html
  head
    title Kzs

    = stylesheet_link_tag "app", :media => "all"
    = javascript_include_tag "library"
    = csrf_meta_tags
    = yield(:head)
  body
    .wrapper.js-dashboard-page
      = render 'shared/page_title'
      = render 'shared/welcome_screen'
      - unless controller?('devise/sessions')
        = yield