var $tasks = $('.js-tasks-order-task-form' + ' input[type=checkbox]');
var $completed_tasks = $('.js-tasks-order-task-form' + ' input[type=checkbox]:checked');
if ($tasks.length == $completed_tasks.length){
    //   if (confirm('Все задачи распоряжения выполнены?')){
    _.each($tasks, function(t) {
        $(t).iCheck('disable')
    })
    // }

    $('.js-orders-new-report-btn').show();
}else{
    $('.js-orders-new-report-btn').hide();
}